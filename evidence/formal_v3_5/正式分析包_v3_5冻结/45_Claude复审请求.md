# v3.2 初审修正后的独立只读复审请求

你是 Fable（Claude Code / Opus 4.6 low），只做独立只读复审，不改文件、不运行模型
调用。你上轮 BLOCK 的理由已记在 `44_Claude_v3_2初审_BLOCK.md`：O3 同样走 Codex，
却没有严格 Schema。

请重新读取当前候选包实际代码和相邻 v3/v3.1 证据，重点核对：

1. O3 是否现在独占 `17_schema_transport_codex_open.json`，该文件与通用 Schema 是否
   真的只差六处冗余的 `type: string`；
2. M3/O3 实际交给 Codex 的所有 `const`/`enum` 节点是否都有显式 `type`，以及是否
   还有明显会触发当前严格结构化输出接口 400 的 Schema 缺口；
3. 合成校准是否确实覆盖 M3 与 O3、各自只许首试，任一失败是否阻断正式编码；
4. 预算是否准确变为 2+32+6+8=48，授权范围、状态重建和旁路防线是否同步；
5. 上轮已通过的 v3.1 零输出锁定、M1/M2 整槽复用、提示/材料/判法/复核/统计不变等
   结论是否仍成立；
6. 初审意见是否被忠实记录，当前包是否可冻结并只先跑两次 Codex 合成校准。

必须给 `PASS` 或 `BLOCK`。若 PASS，列出仍需披露的限制；若 BLOCK，指出具体文件、
路径和会造成的后果。不要因为已经返工一次而从宽。
