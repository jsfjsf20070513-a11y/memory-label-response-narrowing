# v3.4 独立只读审计请求

你是 Fable（Claude Code / Opus 4.6 low），只读审计，不改文件、不运行模型调用。
请实际读取当前候选包、`正式分析包_v3_3冻结/` 和 `正式分析运行_v3_3/`，给 PASS
或 BLOCK。

重点核对：

1. v3.3 M1 五批是否全部有效、能逐字重建完整 144 条 merged；M2 两次是否确实无效，
   且 v3.3 没有完整 review manifest/summary；
2. v3.4 是否只复用完整 M1 槽，不复用 M2 无效输出，不重跑任何编码；
3. 25 份静态 Schema 是否与剩余五槽的实际冻结任务和分批逐项一致，是否从结构上禁止
   缺键、多键、改键；Codex 用到的 const/enum 是否显式有 type；
4. 键固定输出转回旧列表后是否仍走原 `validate_review()` 和原汇总逻辑，是否有批间
   重复/遗漏或选择性复用暗门；
5. 新 CLI、阶段清单、33 次上限、8 次重试、累计最坏 76 的说法是否准确；
6. M1 数组运输与其余槽键固定运输的不对称是否可接受，报告还必须披露什么。

不要因为已经多次返工而从宽；若结构化 Schema 本身可能被 Claude/Codex 拒绝，请明确
指出具体路径。
