# v3.5 独立只读审计请求

你是 Fable（Claude Code / Opus 4.6 low），只读审计，不改文件、不运行正式模型调用。
请实际读取当前候选包、`正式分析包_v3_4冻结/` 和 `正式分析运行_v3_4/`，给 PASS 或
BLOCK。

重点核对：

1. v3.4 的 M2/M3 是否各五批首试有效，能逐字重建完整 merged；M1 是否来自已复验的
   v3.3 完整槽；v3.4 是否没有完整 review manifest/summary；
2. O1 两次失败是否都确为推理前 429，输入、输出 token 为 0，没有模型输出可复用；
3. v3.5 是否只复用完整 M1/M2/M3，不复用 O1 失败，不重跑回答、校准或编码；
4. O1/O2/O3 是否仍使用 v3.4 冻结的任务、五批计划、任务键固定 Schema、原语义校验和
   原汇总，是否有选择性复用、重新分批或人工揭盲暗门；
5. 新 CLI、阶段清单、15 主调用+8 重试=23，以及先前实际 55、最坏累计恰好 78 的口径
   是否准确；额度失败是否也计入实际请求数；
6. M1 数组运输、M2/M3 v3.4 键运输、O1/O2/O3 v3.5 键运输的不对称是否可接受，最终
   报告还必须披露什么。

特别检查：`prepare` 是否能从父运行原始件重建所有复用槽；`review` 是否只可能发出
O1/O2/O3 的 15 批；`aggregate` 是否必须等六槽全集和 review manifest 完整后才能运行。
不要因为额度中断而从宽。
