# v3.3 独立只读审计请求

你是 Fable（Claude Code / Opus 4.6 low），只读审计，不改文件、不运行模型调用。
请实际读取当前候选包与相邻 `正式分析包_v3_2冻结/`、`正式分析运行_v3_2/`，给
PASS 或 BLOCK。

重点核对：

1. v3.2 是否确有 18 份完整规范编码、32 个新编码单元均有效，而 review 两次输出均
   因任务集不全无效且没有 review manifest/summary；
2. v3.3 是否只复制完整编码，不复制无效复核输出，不重跑生成/校准/编码；
3. 每名复核者的任务全集、排序、提示、模型、Schema、判断标准是否不变，只机械切为
   ≤30 条；六名是否恰为 5 批、总计 30 次；
4. 分批合并后是否用原完整任务集做集合相等与去重检查，批间重复/遗漏能否被拦；
5. 新 CLI 是否只有 prepare/review/aggregate，授权、38 次上限、8 次重试和阶段哈希链
   是否不可旁路；
6. 这项编码后修订能否在不解盲的前提下冻结执行，以及报告必须披露什么。

不要因已经多次返工而从宽。指出任何会改变票或允许选择性复用的路径。
