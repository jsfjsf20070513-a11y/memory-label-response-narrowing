# v3.2 独立只读审计请求

你是 Fable（Claude Code / Opus 4.6 low），只做独立只读审计，不改任何文件，也不运行
模型调用。当前目录是正式分析 v3.2 候选包。请实际读取候选包以及相邻的
`正式分析包_v3_1冻结/`、`正式分析运行_v3_1/`，核对后给 PASS 或 BLOCK。

背景：v3.1 第一次 M3 长合成校准在推理前被 Codex 结构化输出接口以 400 拒绝，错误
指向 `properties.method` 只有 `const` 而没有 `type`。v3.2 声称只给 Codex 分表
Schema 的全部 `const`/`enum` 字符串节点补 `type: string`，不改研究材料或尺子。

请重点回答：

1. v3.1 raw 是否确实没有模型输出、没有有效票，且新包是否机械锁定了这段失败史；
2. v3.2 的实际 diff 是否仅限接口兼容与版本/审计链，是否误改了提示、回答、判法、
   复核、统计或 M1/M2 的整槽复用边界；
3. 新 Codex Schema 是否把所有 `const`/`enum` 类型补齐，结构上仍禁止菜单方向填写
   `name/definition`，合并后仍回到原内部格式；
4. 校准首试硬闸、32 次新编码、至多 6 次复核、8 次重试余量、47 次新目录总上限是否
   一致且不可旁路；
5. 是否还有会让 Codex 严格结构化输出在推理前拒绝的明显 Schema 缺口；
6. 是否可以冻结并只重跑 1 次 M3 合成校准。

最后列出必须在报告中披露的限制。不要因为项目方希望继续就从宽。
