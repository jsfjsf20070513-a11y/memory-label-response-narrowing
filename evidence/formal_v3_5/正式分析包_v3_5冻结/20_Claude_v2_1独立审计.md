# Claude Code 对 v2.1 的独立审计

- 完成时间：2026-07-16T10:40:19Z
- Claude Code：2.1.207
- 模型：`claude-opus-4-6`
- 推理强度：low
- 会话：safe mode、无持久化、无工具
- 输入：候选包内的修订说明、预注册、闸门、配置、代码、提示词与测试
- 明确未输入：正式回答、臂映射、实验运行输出
- 性质：冻结前设计审，不计入正式分析请求预算

第一次只读审计因 safe mode 的目录映射错误而没有获得文件内容，明确未形成审计结论。
随后把同一批候选包文本直接作为输入、关闭全部工具，重新独立审计。

## 审计结论

**无阻塞。**

Claude 对五项重点的结论：

1. “零对零判相当”和“已有菜单不得另造同义菜单外项”属于收窄歧义，没有改变
   `math_focus / nonmath_breadth` 的研究对象或统计门槛；
2. v2 与 v2.1 的运行目录、授权文件、授权范围和 `CAL/ENCODE` 单元前缀不同，不能
   拼接 v2 已通过的 M1/M2；
3. 父版验证要求 raw 单元集合恰为三个校准单元、不存在校准通过清单、且不存在任何
   `ENCODE*`，不会漏掉父版已经接触正式回答的情况；
4. 新版仍由 68 次总请求上限和 8 个首试失败单元上限锁住；
5. 正式提示仍只读取 `answers_blinded.json`，非盲映射只在全部编码和复核完成后的
   汇总阶段使用。

审计没有提出需要修改的高价值问题。
