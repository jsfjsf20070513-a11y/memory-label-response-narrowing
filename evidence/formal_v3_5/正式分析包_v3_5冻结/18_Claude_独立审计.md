# Claude Code 独立审计记录

- 时间：2026-07-16T10:33:13Z
- 通道：Claude Code 2.1.207
- 模型：`claude-opus-4-6`
- 推理强度：low
- 会话：独立、无持久化、safe mode
- 权限：只允许 Read / Grep / Glob；明确禁止读取候选包目录之外的文件
- 性质：冻结前设计审，不属于正式分析 68 次调用预算；没有读取正式回答

## 审计问题

1. 校准是否可被绕过；
2. 正式编码是否泄露臂映射；
3. 源回答或旧票能否被改写、覆盖或混入；
4. `6 + 48 + 至多 6`、重试至多 8、总尝试不超过 68 是否被程序锁住；
5. 运输格式展开是否改变旧判法；
6. 阶段证据链能否复验；
7. 生成后分析修订是否被误写成完整预注册。

## 结论

Claude 审计结论：**无阻塞**。

它确认：

- `encode / review / aggregate` 都必须先通过校准阶段清单与 PASS 报告；
- 旧 `pipeline.py` 的命令行入口已关闭，不能绕过校准；
- 正式编码提示只加载盲化回答，不渲染臂映射；
- 源回答、源生成清单、新运行目录和旧失败 raw 彼此分离且有哈希保护；
- 每次真实请求前都会检查 68 次总上限，第一次失败单元上限为 8；
- 运输输出会先展开为旧规范格式，再过旧 Schema 与旧语义校验；
- 文档明确把 v2 定位为“生成后、零有效主编码、保持盲化的分析运输修订”。

## 审计提醒及处理

Claude 提醒：若旧规范格式的 `decisive` 对象要求 `tag`，当前展开结果缺少该字段会让
校准自锁。

本方随后机械检查：

- `12_schema_coding_menu.json` 的 decisive 必填字段为
  `answer/local_id/name/definition/evidence/occurrence`；
- `13_schema_coding_open.json` 相同；
- 两者都不含 `tag`，而标签一致性由已登记方向和旧语义校验器检查。

因此该提醒已排除，无需修改运输格式。
