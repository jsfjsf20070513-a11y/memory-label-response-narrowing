# Claude Code / Opus 4.6 对 v3.2 的独立复审：PASS

- 模型：Claude Code `claude-opus-4-6`，low；
- 模式：无会话持久化、plan、只读工具；
- 请求：`45_Claude复审请求.md`；
- 结论：**PASS，可冻结并先跑 M3/O3 两次首试合成校准。**

## 六项核对

1. O3 独占 `17_schema_transport_codex_open.json`；路由为 M3→分表严格 Schema、
   O3→开放严格 Schema、其余→通用 Schema。O3 严格副本与通用 Schema 的差异恰为
   六个 `enum` 节点增加 `type: string`，有递归剥离测试。
2. M3 七个、O3 六个 `const`/`enum` 节点均有显式字符串类型；递归测试覆盖两条
   Codex 实际路径，未见残留的同类 Schema 缺口。
3. `calibration_slots=[M3,O3]`，两槽各自 `max_attempts=1`；任一失败都会异常退出，
   正式编码由 `require_calibration_pass()` 阻断。
4. 新目录预算为 `2+32+6+8=48`；授权范围、v3/v3.1 状态哈希链、请求上限与旁路
   防线同步。
5. v3.1 零模型输出事实、M1/M2 整槽复用、提示/材料/判法/复核/统计不变仍成立。
6. `44_Claude_v3_2初审_BLOCK.md` 忠实记录了初审理由，当前修正与其要求对应。

## 报告必须披露的限制

- v3.2 是生成后修订，不是一次完成实验；必须完整披露 v3→v3.1→v3.2 失败链和票
  复用史。
- M1/M2 复用票在 v3.2 设计前已生成，不构成独立复制。
- Codex 要求枚举/固定值显式类型，是从 v3.1 的真实 400 错误得到的接口经验事实，
  不是本项目查阅官方文档后得到的声明。
- 通用 Schema 故意保持原字节，不给 Claude 槽补冗余类型，以维持复用边界。
- 合成校准只用 S2，正式编码还涉及 S8/S10。

本页记录的是第二次成功复审；前一次因 Max 额度中断且无模型意见，见
`46_Claude复审额度中断.md`，不得计作审计票或正式研究调用。
