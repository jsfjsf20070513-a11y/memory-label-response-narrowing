# 正式分析 v3.5 闸门

用户原文 `go` 授权继续当前正式实验。v3.4 已完成 M2/M3，O1 第一批两次均在推理前
被 Claude Max 额度拒绝；两次输入、输出 token 都是 0，没有模型判断。

本授权允许：

- 机械复验并复制 v3.2 的 18 份编码、v3.3 的完整 M1、v3.4 的完整 M2/M3；
- 锁定但不复用 O1 的两份 429 raw；
- 额度重置后新跑 O1/O2/O3 共 15 个任务键固定复核单元；
- 在新目录 23 次上限内使用至多 8 次重试；
- 六槽复核全集校验通过后运行原冻结汇总。

它不允许重跑或改写回答、校准、编码、M1/M2/M3；不允许把额度拒绝当模型输出、手补
任务、选择性复用未完成槽、跳过复核汇总，或在汇总前人工揭盲。

冻结前 `../正式分析运行_v3_5/00_open_analysis_v3_5.json` 必须不存在。Claude Max
重置、Opus 4.6 独立审计 PASS、冻结核验通过后，才可登记闸门并严格按
`prepare → review → aggregate` 执行。
