# Repro Pack: 正式分析 v3.5

## Claim

本证据包支撑以下有边界主张：冻结的数学背景臂相对无背景臂，在 12 个主块中有 8 个被菜单法与开放法共同判为“更数学且非数学方向更少”，反向 0，单侧精确 `p=0.00390625`。

## Code State

- Repo: `jsfjsf20070513-a11y/memory-label-response-narrowing`
- Branch: `main`
- 冻结分析包清单 SHA-256: `f36c62570c26c4f577559d86932c583ee997fdcfa23c4a1a1da466b86884e6e0`
- 关账结果于 2026-07-22 生成；本仓库只对其作字节一致复建，不重新调用模型。

## Environment

- 原始运行：Python 3.14.0；
- 锁定依赖：见 `evidence/formal_v3_5/正式分析包_v3_5冻结/requirements.lock.txt`；
- 复建汇总不会调用 Claude、Codex 或任何网络 API。

## Inputs

| 输入 | 路径 | 公开性 | 摘要 / 哈希 |
|---|---|---|---|
| 48 份盲化回答 | `evidence/formal_v3_5/正式分析运行_v3_5/answers_blinded.json` | 脱敏研究输入 | `3b0ac359…f3167` |
| 未盲化臂映射 | `evidence/formal_v3_5/正式分析运行_v3_5/answers_unblinded.json` | 已关账实验数据 | `c9dc7369…642726` |
| 18 份主编码 | `evidence/formal_v3_5/正式分析运行_v3_5/effective/coding/` | 脱敏研究输入 | 由 `manifest_coding.json` 锁定 |
| 6 份复核合并结果 | `evidence/formal_v3_5/正式分析运行_v3_5/effective/review/` | 脱敏研究输入 | 由 `manifest_review.json` 锁定 |

原始账户聊天导出、PII、判官身份和未关账人类答案不在包内。

## Commands

```bash
PYTHONDONTWRITEBYTECODE=1 sh scripts/verify_formal_v3_5.sh
```

脚本重新执行冻结汇总，随后对三个结果文件计算 SHA-256。

## Outputs

| 输出 | 期望 SHA-256 | 用途 |
|---|---|---|
| `summary.json` | `d49be125db9ac42ea860a51f580270f93f2e83c3f34b41f48dba4f8e1a5062bf` | 预注册汇总 |
| `audit.json` | `a9117b1d839c57adbde7598e0229f32694d145954ce1c2692d2453e78028683f` | 逐块、逐方法审计 |
| `manifest_results.json` | `709d9d3d23f32b3aa0b642a15915e23f7544310c3fd5eba53a70ea3a96e219b2` | 协作脱敏快照的结果阶段清单 |

## Pass Criteria

- 汇总命令退出码为 0；
- 三个输出哈希与上表逐字一致；
- 仓库隐私检查通过；
- 复建前后 Git 跟踪文件无差异。

## Reproduction Log

- 2026-07-24：将冻结包和 v3.5 运行目录单独复制到隔离临时目录，不带 v3.0–v3.4 父目录；
- 执行 `python3 analysis_v2.py aggregate` 成功；
- 为不暴露本机用户路径，5 份 O3 原始运输外壳共替换 20 处用户主目录前缀，并重算 `manifest_review.json` 与 `manifest_results.json`；判断内容、有效输出、`summary.json` 和 `audit.json` 均字节不变；
- 三个结果哈希分别为上表期望值，证明最终汇总不依赖未入库的旧候选版。

## Known Limits

- 这个轻量包复建“已回收的冻结编码和复核如何产生结果”，不重放 70 次订阅模型请求；
- 运输修补的完整 v3.0–v3.4 失败链保留在本地原项目，本协作库只收录关键审核记录；
- 冻结包原始 `32_tests.py` 共 72 项：在本精简快照中 62 项通过，10 项因故意未收录 v3.0–v3.4 父包/父运行而报缺文件。因此不将整套 72 项测试列为本协作库验收门槛；
- 本包关账时证据仍是 AI 生成、AI 编码、AI 复核；人类尺子校验当时尚未关账。
  后续三人结果已于 2026-07-24 独立关账，见
  `docs/human-validation/10_人类校验关账报告_2026-07-24.md`，不回改本包。

## Conclusion

**当前协作包对正式 v3.5 统计汇总可复现，对完整历史管线只是部分可复现。**它不包含账户原始数据，也不支持重放订阅模型生成过程。
