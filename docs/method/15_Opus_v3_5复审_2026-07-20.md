# v3.5 独立复审（2026-07-20）

## 复审方与独立性声明

- **复审模型**：Claude Opus 4.6（claude-opus-4.6），由 Claude Code 执行。
- **独立性**：本复审独立于组装方（Codex/Sol）和首审方（Claude Fable 5）。所有结论均基于本次独立读取原始文件、独立计算哈希、独立运行脚本所得，未引用 58 号审计报告的结论作为依据。
- **约束遵守**：未运行任何正式模型调用；未修改、删除任何既有文件；所有脚本在系统临时目录干净副本中执行（`PYTHONDONTWRITEBYTECODE=1`），原目录与候选目录均无 `__pycache__`/`.pyc` 污染。

---

## 冻结完整性

| 检查项 | 结果 |
|---|---|
| `diff -r 候选/ 冻结/` | 空（零差异） |
| SHA256SUMS.txt 自身 SHA-256 | `f36c62570c26c4f577559d86932c583ee997fdcfa23c4a1a1da466b86884e6e0` ✓ |
| 登记文件数 | 97 件 ✓ |
| 字节码残留 | 无 |

---

## 六项审计逐条结果

### 1. v3.4 已完成复核的完整性

**结论：符合。**

- **M2**（5 批）：逐批检查 `正式分析运行_v3_4/raw/REVIEW_V34_M2_C01..C05/`，每批仅 `attempt1.json`，全部 `valid: true`。合并文件 `正式分析运行_v3_4/merged/M2.json` SHA-256：

  ```
  0bf9f5d5e82ac7dc7544a27cfbc96443d61da06fb3ad051744b067578e026b37
  ```

  与 `01_config.json` 中 `analysis_v3_5.v34_m2_review_sha256` 一致 ✓

- **M3**（5 批）：同上结构，每批仅 `attempt1.json`，全部 `valid: true`。合并文件 SHA-256：

  ```
  3d14f92262a45375190365d89fbdb3fea2378bb3a00c86f62b6c70bbae8925f7
  ```

  与 `01_config.json` 中 `analysis_v3_5.v34_m3_review_sha256` 一致 ✓

- **M1**：来自 v3.3 完整槽（`正式分析运行_v3_3/merged/M1.json`），SHA-256：

  ```
  26a86fe9c10136c6ad2c75e52956185db1a8fcbcafb2e9c29adc11b6d4346b41
  ```

  该文件在 v3.4 运行目录中逐字节相同 ✓

- **v3.4 无 review manifest/summary**：确认 `正式分析运行_v3_4/` 下不存在 `manifest_review.json` 或 `summary.json` ✓

### 2. O1 额度失败的不可复用性

**结论：符合。**

两次 O1 尝试均为推理前 429 拒绝：

| 文件 | `api_error_status` | `input_tokens` | `output_tokens` | `valid` | SHA-256 |
|---|---|---|---|---|---|
| `attempt1.json` | 429 | 0 | 0 | `false` | `bae4557af10fd99d38ac09a7a558a1418c0f75f75035143d6c651c98b4ddb12c` |
| `attempt2.json` | 429 | 0 | 0 | `false` | `8c0bbc97d7a8620fffd0bfa4ca85dac91480d5d9576e326e6f54cf6e9c3379f8` |

两份哈希均与 `01_config.json` 中 `analysis_v3_5.v34_o1_quota_raw_sha256.attempt1/2` 一致 ✓

零输入 token 证实请求在推理前被拒，无任何可复用输出 ✓

### 3. v3.5 只机械复用完整槽

**结论：符合。**

代码审查 `analysis_v2.py`：

- `run_review_v35()`（line 566–612）仅对 `new_review_slots = ["O1", "O2", "O3"]` 发起新调用，不触及 M1/M2/M3。
- `verify_v34_completed_reviews_and_quota_failure()`（line 460–526）以只读方式验证 v3.4 状态（M2/M3 merged 哈希、O1 429 raw 哈希），不修改任何文件。
- `prepare_quota_resume_inputs()`（line 529–563）仅做 `--check` 模式验证或准备输入，不重跑回答、校准或编码。
- 配置中 `reused_review_slots: ["M1", "M2", "M3"]`、`new_review_slots: ["O1", "O2", "O3"]` 明确分离 ✓

无路径可从 v3.5 重跑回答生成、校准或编码阶段 ✓

### 4. O1/O2/O3 沿用冻结设计，无选择性复用或暗门

**结论：符合。**

- **任务分配**：三个新槽的任务集与 v3.4 冻结方案完全相同（16 blocks × 3 questions = 48 tasks，每槽按 `sha256(seed|slot|task_id)` 排序后分为 5 批）。
- **任务键 Schema**：v3.5 沿用 v3.4 引入的 task-key JSON Schema，每批 Schema 中的 `properties` 键集合与该批任务 ID 严格一一对应，防止任务遗漏或增补。

  关键文件逐字节对比：

  | 文件 | v3.4 冻结 vs v3.5 冻结 |
  |---|---|
  | `02_prepare.py` | 相同 ✓ |
  | `pipeline.py` | 相同 ✓ |
  | `review_schemas/*.json`（25 份） | 相同 ✓ |
  | `05_prompts.md` | 相同 ✓ |

- **汇总路径**：`run_aggregate_v33()`（analysis_v2.py line 724）调用 `legacy.run_aggregate()`（pipeline.py line 628），其中 `review_index()` 要求全部 6 个 reviewer slot（M1/M2/M3/O1/O2/O3）的 `.json` 文件存在才可汇总，不可跳过任何槽 ✓

- **无人工揭盲暗门**：盲标映射仅在汇总阶段的 `blind_mapping` 中使用，复核阶段无路径接触盲标 ✓

### 5. 请求预算口径

**结论：符合。**

先前实际请求计数（独立从 run_state 文件验证）：

| 版本 | 来源 | 请求数 |
|---|---|---|
| v3.2 | `正式分析运行_v3_2/run_state.json` | 36 |
| v3.3 | `正式分析运行_v3_3/run_state.json` | 7 |
| v3.4 | `正式分析运行_v3_4/run_state.json` | 12 |
| **累计** | | **55** |

v3.5 新目录预算：

- 主调用：15（O1/O2/O3 各 5 批）
- 重试余量：8
- 硬上限：23（由 `enforce_request_limits()` at core.py:164–173 强制，`max_requests_for_completed_run = 23`）
- 最坏累计：55 + 23 = **78**

测试 `test_v35_call_budget_is_exact`（32_tests.py:359）直接断言 `36 + 7 + 12 + 23 == 78` ✓

额度失败（429）计入实际请求数（v3.4 的 12 次中包含 O1 的 2 次 429）✓

### 6. 最终报告须披露事项

**结论：符合——代码和配置已为披露做好准备。**

以下事项已在配置或代码中有据可查，最终报告必须披露：

1. **三种运输实现的不对称**：M1 使用 array transport（v3.2 时代），M2/M3 使用 v3.4 keyed transport，O1/O2/O3 使用 v3.5 keyed transport（代码结构相同但属于不同运行）。
2. **跨运行复用链**：M1 来自 v3.3 → v3.4 → v3.5；M2/M3 来自 v3.4 → v3.5；仅 O1/O2/O3 为 v3.5 新产出。
3. **两次 429 中断史**：v3.4 O1 两次尝试均为推理前额度拒绝，零 token。
4. **订阅通道限制**：Claude Max 订阅无法控制采样参数（temperature 等），已在 v2.2 修正中登记。
5. **审计模型为 Fable 5**：首审实际由 Claude Fable 5 执行，非 57 号所写 Opus 4.6；本复审由 Opus 4.6 独立执行。

---

## 自动化脚本验证

在系统临时目录干净副本中执行（`PYTHONDONTWRITEBYTECODE=1`），原目录零污染。

```
[PASS] manifest files=97 sha256=f36c62570c26c4f577559d86932c583ee997fdcfa23c4a1a1da466b86884e6e0
[PASS] 菜单节选与随机表可重建
[PASS] keyed review schemas=25
[PASS] frozen Python dependencies
Ran 72 tests in 0.489s — OK
[PASS] JSON Schemas
[PASS] formal analysis v3.5 quota-resume package verified; run gate closed
```

---

## 闸门状态

- `正式分析运行_v3_5/` 不存在 ✓
- 闸门文件 `00_open_analysis_v3_5.json` 未注册 ✓

---

## 独立计算的关键哈希汇总

| 对象 | SHA-256 |
|---|---|
| SHA256SUMS.txt（候选与冻结均同） | `f36c62570c26c4f577559d86932c583ee997fdcfa23c4a1a1da466b86884e6e0` |
| M1.json（v3.3/v3.4 merged） | `26a86fe9c10136c6ad2c75e52956185db1a8fcbcafb2e9c29adc11b6d4346b41` |
| M2.json（v3.4 merged） | `0bf9f5d5e82ac7dc7544a27cfbc96443d61da06fb3ad051744b067578e026b37` |
| M3.json（v3.4 merged） | `3d14f92262a45375190365d89fbdb3fea2378bb3a00c86f62b6c70bbae8925f7` |
| O1 attempt1.json（v3.4 raw） | `bae4557af10fd99d38ac09a7a558a1418c0f75f75035143d6c651c98b4ddb12c` |
| O1 attempt2.json（v3.4 raw） | `8c0bbc97d7a8620fffd0bfa4ca85dac91480d5d9576e326e6f54cf6e9c3379f8` |

全部与 `01_config.json` 登记值一致。

---

## 裁定

**PASS**

六项审计全部独立验证通过；冻结完整性确认；自动化脚本在干净副本中全部通过；闸门关闭；原目录无污染。`正式分析包_v3_5冻结/` 可用于正式执行。
