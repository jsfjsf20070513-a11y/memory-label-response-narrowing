# v3.5 独立审计 PASS 与冻结(2026-07-20)

## 交接与审计方

Codex(Sol)组装的 `正式分析包_v3_5候选/` 由 Claude Code 独立只读审计。
**审计模型变更披露:实际模型为 Claude Fable 5(claude-fable-5),不是 57 号请求
所写的 Opus 4.6;** 审计独立于组装方、未运行任何正式模型调用,测试与核验均在
scratchpad 干净临时副本中执行。全文见包内 `58_Claude_v3_5独立审计_PASS.md`。

## 审计结果

按 57 号六项逐一核对,全部符合:

1. v3.4 M2/M3 十批均 attempt1 有效,合并文件哈希与 13 号记录一致,可逐字节重建;
   M1 来自 v3.3 完整槽;v3.4 无 review manifest/summary。
2. O1 两次失败均为推理前 429,输入输出 token 为 0,`valid:false`,无可复用输出;
   raw 哈希与登记一致。
3. v3.5 只整槽复用完整 M1/M2/M3,不复用 O1 失败,不重跑回答、校准或编码。
4. O1/O2/O3 沿用 v3.4 冻结的任务、五批计划、任务键 Schema(逐字节相同)、原语义
   校验与原汇总;无选择性复用、重新分批或人工揭盲暗门。
5. 预算口径准确:先前实际 36+7+12=55,新目录 15 主调用+8 重试=23 由
   `enforce_request_limits()` 强制,最坏累计恰 78;额度失败计入实际请求数。
6. 三种运输实现的不对称可接受,但最终报告必须披露(运输不对称、跨运行复用链、
   两次 429 中断史、订阅通道限制、审计模型为 Fable 5)。

现场:干净副本 `99_verify.sh` 退出码 0(96 件清单、菜单/随机表/25 份 Schema 可
重建、72 项测试、闸门关闭);审计报告写入后重登清单为 97 件,
清单 SHA-256 `f36c62570c26c4f577559d86932c583ee997fdcfa23c4a1a1da466b86884e6e0`,
干净副本复核仍退出码 0。

## 冻结

`正式分析包_v3_5冻结/` 为候选包逐字节复制(diff -r 为空,无字节码残留),
清单自身 SHA-256 同上。`正式分析运行_v3_5/` 仍不存在,调用闸门关闭。

## 下一步

等待用户本轮原文 `go`;登记后严格按 `prepare → review → aggregate` 执行
O1/O2/O3 十五批。六槽全集通过前不得汇总、不得揭盲。
