# 从零看懂这个实验

> 给第一次进入项目的人。你没有漏看：旧入口确实没有把“题目、重复次数和文件编号”
> 分开讲清楚。

## 先纠正最容易混淆的一点

正式实验不是“从 S1 到 S12 共 12 道题”。

这里有三种完全不同的编号：

| 看见的编号 | 它是什么 |
|---|---|
| `F1—F10`、`S1—S10` | 早期候选题池里的题目名字 |
| `S8'`、`S10'` | 原 S8、S10 改过措辞后的版本；撇号只表示“修订版” |
| 12 个主块 | 两道正式主问题各独立生成 6 次，`2 × 6 = 12`，不是 12 道题 |
| `H01—H12` | 人类盲判时重新随机后的 12 对回答编号，也不是新题 |

代码里的 JSON 为了避免特殊字符，把 `S8' / S10'` 简写成 `S8 / S10`。文字内容
相同；这是实现命名和论文命名不统一，容易误解。

## 正式实验到底用了什么

### 三种背景

- `N`：不告诉模型任何用户背景；
- `G`：只告诉模型“用户是一名本科生”；
- `M`：只告诉模型“用户是数学系本科生”。

每次都是全新对话。除此之外，问题、模型和调用方式保持相同。

### 三道实际使用的问题

| 用途 | 题目 | 独立重复 |
|---|---|---:|
| 主检验 | `S2`：旧物坏了，人们怎样想要不要修？ | 6 |
| 主检验 | `S10'`：为什么人在很多选择间犹豫，决定后仍会后悔？ | 6 |
| 单列复现 | `S8'`：为什么人会依赖推荐系统，即使知道选择可能被影响？ | 4 |

每个重复块都生成 `N/G/M` 三份回答，所以总数是：

```text
S2：   6 × 3 = 18 份
S10'： 6 × 3 = 18 份
S8'：  4 × 3 = 12 份
总计：          48 份
```

主结论只用 `S2 + S10'` 的 12 个块。`S8'` 是试跑后挑出的题，只能单独检查能否
复现，不能混进主检验的数字。

## 一块回答怎样被判分

我们不把“出现公式”直接当成变窄。每个块分别问两件事：

1. 数学背景回答是否把组织重心放得更偏数学？
2. 无背景回答是否包含更多实质不同的非数学方向？

两件事同时成立才记 `+1`。两件事同时反向才记 `-1`，其余记 `0`。

回答先被隐藏分组，再交给两套判法：

- 菜单法：按事先冻结的方向清单找证据；
- 开放法：不看菜单，自行概括方向。

两套判法内部都由多个独立编码结果合成。主结果采用两套判法共同支持的方向，防止一把
尺子单独决定结论。

## `p` 是什么

这里的 `p` 可以先按“硬币检验”理解。

正式主检验里有 8 个有明确方向的块，结果全都朝同一个方向，反向 0 个。如果现实中
根本没有稳定方向，每个非零块像抛硬币一样各有一半机会朝任一边，那么 8 次全部同向的
概率是：

```text
(1/2)^8 = 0.00390625
```

这就是主结果报告的单侧精确 `p=0.00390625`。

它不表示“结论有 99.6% 概率为真”，也不表示效应很大。它只表示：在“两个方向本来
一样可能”的假设下，看到 8 比 0 这么整齐的方向并不常见。

人类校验是 10 个正向、0 个反向，所以：

```text
(1/2)^10 = 0.0009765625
```

## 从头到尾发生了什么

1. **个人感受**：AI 知道“我是数学系学生”后，回答似乎总被拉向数学。
2. **先停掉错题**：早期 E-2 测的是“背景会不会压过明确命令”，和原始感受不一致，
   因此暂停，没有拿错误实验硬跑。
3. **把问题缩小**：只测“更数学”是否与“其他方向更少”同时出现。
4. **做极小试跑**：只校准题目、编码尺子和运行管线，不把结果当正式证据。
5. **结果前写规则**：冻结题目、三种背景、样本量、判分方法和通过门槛。
6. **正式生成**：Claude Opus 4.6 在独立无状态会话中生成 48 份回答。
7. **盲化判读**：菜单法和开放法分别编码、复核，再按冻结程序汇总。
8. **正式结果**：主检验为正向 8/12、反向 0/12，`p=0.00390625`。
9. **人类校验**：三名不知分组的人判断同样的 12 对主回答；正向 10、反向 0，
   与 AI 逐块一致 10/12。

## 完整数据在哪里

按下面顺序看，不需要在编号文件里来回跳：

1. 问题起点：[`docs/project-history/研究说明_大白话.md`](docs/project-history/%E7%A0%94%E7%A9%B6%E8%AF%B4%E6%98%8E_%E5%A4%A7%E7%99%BD%E8%AF%9D.md)
2. 题池和撇号来源：[`docs/method/08_合稿修订版.md`](docs/method/08_%E5%90%88%E7%A8%BF%E4%BF%AE%E8%AE%A2%E7%89%88.md)
3. 正式实验预注册：[`docs/method/05_预注册修订稿_v0_2.md`](docs/method/05_%E9%A2%84%E6%B3%A8%E5%86%8C%E4%BF%AE%E8%AE%A2%E7%A8%BF_v0_2.md)
4. 冻结题目与分析代码：`evidence/formal_v3_5/正式分析包_v3_5冻结/`
5. 48 份回答、编码、复核与汇总：`evidence/formal_v3_5/正式分析运行_v3_5/`
6. 正式结果：[`docs/results/16_正式实验v3_5结果_人话版_2026-07-22.md`](docs/results/16_%E6%AD%A3%E5%BC%8F%E5%AE%9E%E9%AA%8Cv3_5%E7%BB%93%E6%9E%9C_%E4%BA%BA%E8%AF%9D%E7%89%88_2026-07-22.md)
7. 人类校验：[`docs/human-validation/10_人类校验关账报告_2026-07-24.md`](docs/human-validation/10_%E4%BA%BA%E7%B1%BB%E6%A0%A1%E9%AA%8C%E5%85%B3%E8%B4%A6%E6%8A%A5%E5%91%8A_2026-07-24.md)

想直接核数字，在仓库根目录运行：

```bash
PYTHONDONTWRITEBYTECODE=1 sh scripts/verify_formal_v3_5.sh
```

## “完整”到什么程度

仓库包含正式 v3.5 最终分析需要的脱敏回答、编码、复核、汇总、规则和代码，可以重新
算出最终数字。

仓库没有放入整个 1.9GB 本地研究史：原始聊天账户导出、Claude 会话恢复包、私密人类
判卷、管理员身份映射以及部分早期失败候选包都不上传。Claude Max 订阅通道也不能保证
今天重新生成逐字相同的 48 份回答。

所以准确说法是：

> 最终 v3.5 的统计汇总可以复现；从第一次聊天到每个失败版本的完整考古没有全部公开，
> 模型生成阶段也不能从订阅通道逐字重放。

如果读完这页仍觉得某一步断了，请直接指出“从哪一步到哪一步断了”。这本身就是有价值
的协作反馈。
