# 项目工作约定

这份文件是仓库内的方法与数据红线。人类协作者和 AI 编码/写作代理都必须遵守。

## 研究口径

1. 永远把“观察”和“解释”写成两层。
2. 当前只能说现象在冻结的模型、题目、通道和背景注入方式下存在。
3. 不得把输入输出差异写成已证明的模型内部机制。
4. 不得把“更数学”自动等同于“更糟”；价值判断需要另外的用户证据。
5. 试跑结果永久标记为 `PILOT_ONLY`，不得与正式 v3.5 结果混写。

## 冻结证据

- `evidence/formal_v3_5/` 默认只读。
- 复现只允许重新生成字节一致的已登记输出；不得改票、改回答、改映射或事后改判据。
- 新模型、新题目、新分析或新判官必须另建目录，先写判据再看结果。
- 复现失败必须报错并留记录，不得静默改到能跑。

## 人类盲判

- 3 名判官已于 2026-07-24 全部回收并按冻结汇总器关账，状态为
  `calibration_supported`。
- 仓库只保留不含个人票、自由文本理由和真实身份的脱敏汇总。
- 管理员映射、原始 CSV、真实身份对照和完整逐判官输出始终不得提交。
- 本轮不追加第四名判官；任何新的校验必须另立预注册，不能回改本轮结果。

## 隐私与提交边界

绝不提交：原始聊天导出、姓名/学号/联系方式、密码或 API key、本机恢复压缩包、未授权的论文 PDF、管理员盲化映射、未关账人类答案。不使用 `git add -f` 绕过 `.gitignore`。

## Git 协作

- 从 `main` 开短分支，建议命名 `paper/<topic>`、`analysis/<topic>` 或 `docs/<topic>`。
- 一个 PR 只承载一个可独立审查的事实变更。
- 证据、统计数字或结论口径的变更必须在 PR 中给出文件路径和可复算命令。
- 不得 force-push `main`，不得在未经项目负责人确认时重写或删除研究历史。
