# 协作说明

先读 [`COLLABORATION.md`](COLLABORATION.md)。这里的协作包括后续研究推进，不只包括
写作和提交 PR。Issue 里的提问、反对、重新解释问题和共同决定下一步，都是正式贡献。

## 第一次参与

1. 先读 `START_HERE.md`，随时在 Issue 里问；不用等“全看完”才开口。
2. 用自己的话说目前理解到哪里。复述不一致时，大家先把问题讲清。
3. 再读 `README.md` 和 `HANDOFF_2026_07_24_项目协作_TO_LINGYUNLEO.md`。
4. 想核数字时，先运行 `python3 scripts/check_result_hashes.py`；只有本机 CLI 与冻结登记
   版本一致时，再运行 `PYTHONDONTWRITEBYTECODE=1 sh scripts/verify_formal_v3_5.sh`。
5. 阅读笔记和第一个 PR 都是可选的讨论载体，不是加入项目的资格考试。

## 从讨论到实验

1. Issue 里先用人话写清本轮只问什么。
2. 参与者分别提出方案和反例，不要求先统一。
3. 收敛后再冻结问题、样本、判据、预算和失败分支。
4. 只有获得用户明确开跑授权后才产生新的模型调用。
5. 结果出来后先各自解释，再共同写结论。
6. 最后把共同理解翻译成论文语言。

## PR 最小要求

- 标题说明是 `paper`、`analysis` 还是 `docs`。
- 正文写清“改了什么”、“为什么”、“用什么证据支撑”。
- 改动数字、图表或结论时，必须附复现命令。
- 提交前必须运行 `scripts/check_repo_privacy.py` 和 `scripts/check_result_hashes.py`。
- `scripts/verify_formal_v3_5.sh` 任何时候都可以运行，它自己分主次：隐私边界、已存
  结果哈希、冻结证据未被改动是主检查，不过即失败；从冻结包逐字节重算是次检查，
  因 CLI 版本与开跑登记不符而做不成时，打印 `[SKIP]` 并如实报告，整体仍算通过。
  这不是绕过：跳过时会明写“本次未完成重算”，且已存结果由主检查独立确认。
  正式发表前的完整复验，请还原登记的 CLI 版本后加 `--strict` 重跑；该模式下版本
  漂移一律判失败。每次新的版本漂移仍应在 `docs/repro/` 追加一条记录。

## 写作约定

- 每个术语第一次出现时，先或紧跟一句人话解释。
- 一段只承担一个主张。
- 数字紧跟证据文件路径，不靠记忆填写。
- 未验证的机制使用“可能”、“一种解释”，不写成事实。
- 反例和失败结果不得删掉：S8' 复现失败、AI-only 判读、样本窄和运输修补历史都是论文必写限制。
- 如果一段只有作者能解释清楚，先退回讨论，不用术语遮住理解分歧。
