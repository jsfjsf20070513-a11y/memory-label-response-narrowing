# 协作说明

先读 [`COLLABORATION.md`](COLLABORATION.md)。这里的协作包括后续研究推进，不只包括
写作和提交 PR。Issue 里的提问、反对、重新解释问题和共同决定下一步，都是正式贡献。

## 第一次参与

1. 先读 `START_HERE.md`，随时在 Issue 里问；不用等“全看完”才开口。
2. 用自己的话说目前理解到哪里。复述不一致时，大家先把问题讲清。
3. 再读 `README.md` 和 `HANDOFF_2026_07_24_项目协作_TO_LINGYUNLEO.md`。
4. 想核数字时，先运行 `python3 scripts/check_result_hashes.py`；只有本机 CLI 与冻结登记
   版本一致时，再运行 `PYTHONDONTWRITEBYTECODE=1 sh scripts/verify_formal_v3_5.sh`。
5. 阅读笔记和第一个 PR 都是可选的讨论载体，不是加入项目的资格考试。

## 从讨论到实验

1. Issue 里先用人话写清本轮只问什么。
2. 参与者分别提出方案和反例，不要求先统一。
3. 收敛后再冻结问题、样本、判据、预算和失败分支。
4. 只有获得用户明确开跑授权后才产生新的模型调用。
5. 结果出来后先各自解释，再共同写结论。
6. 最后把共同理解翻译成论文语言。

## PR 最小要求

- 标题说明是 `paper`、`analysis` 还是 `docs`。
- 正文写清“改了什么”、“为什么”、“用什么证据支撑”。
- 改动数字、图表或结论时，必须附复现命令。
- 提交前必须运行 `scripts/check_repo_privacy.py` 和 `scripts/check_result_hashes.py`。
- `scripts/verify_formal_v3_5.sh` **默认严格**：任何一项不过都失败，包括从冻结包
  逐字节重算未完成。复现失败必须报错，这一条不放松。
- 只有在确知失败是 CLI 版本漂移、且愿意接受不完整校验时，才显式加
  `--allow-version-drift`。该开关只接受格式明确的纯版本漂移错误；任何其他失败或
  混合错误一律非零退出。降级成功时输出标为 `[PARTIAL]`，不是 `[PASS]`。
- 注意 `check_result_hashes.py` 只能确认已存结果的字节未漂移，**不能**证明当初的
  汇总逻辑或结果正确；后者只有重算能验证。因此它通过不构成跳过重算的理由。
- 每次新的版本漂移仍应在 `docs/repro/` 追加一条记录。

## 写作约定

- 每个术语第一次出现时，先或紧跟一句人话解释。
- 一段只承担一个主张。
- 数字紧跟证据文件路径，不靠记忆填写。
- 未验证的机制使用“可能”、“一种解释”，不写成事实。
- 反例和失败结果不得删掉：S8' 复现失败、AI-only 判读、样本窄和运输修补历史都是论文必写限制。
- 如果一段只有作者能解释清楚，先退回讨论，不用术语遮住理解分歧。
- 改动 `scripts/verify_formal_v3_5.sh` 时必须同时运行
  `sh scripts/test_verify_fault_injection.sh`。该测试**全部在 mktemp 沙箱中的仓库
  副本上执行,不触碰真实冻结目录**(真实证据零接触由首尾两次 lstat 清单比对托底),
  共 11 项断言:被忽略污染检出(须匹配精确错误信息,不认"随便什么非零退出")、
  无关错误对照、干净路径阴性对照、临时文件专属前缀无残留(容忍 OS 噪声)、
  运行中注入 symlink、运行中只改 mode、检查-基线竞态(git shim 复现)、
  运行中写入 ignored 文件、无 `.git` 副本 fail closed、零接触守卫自检、
  真实证据零接触确证。
  冻结目录中存在任何被忽略的文件都是**硬失败**,不是警告;git 命令失败一律
  fail closed。校验器前提:**验证期间独占工作树**,不抵抗外部并发写。

## 合并阻断项(红线类,任何时候优先于复核边界)

以下四类发现**一律阻断合并**,不适用"措辞类不阻断"规则:

1. **隐私**:任何真实姓名、学号、邮箱、手机号、聊天导出、逐判官可归因信息进入
   工作树、提交元数据或新提交历史;
2. **安全**:凭证、密钥、令牌;
3. **伦理**:未经同意的人类数据、超出授权范围的引用;
4. **冻结证据**:对任何冻结包、票、映射或已关账结果的改动。

**提交身份规定**:所有克隆必须设置仓库级化名身份——
`git config user.name <化名>` + `git config user.email <化名>@users.noreply.github.com`。
隐私检查会对 HEAD 的非化名邮箱硬失败。

## 复核边界(2026-07-29 起生效,项目负责人裁定)

七轮复核(46 条意见)的经验固化为以下规则,目的是让复核服务于主线,而不是替代主线:

1. **合并闸门只看功能性意见**:会改变脚本行为、数据、统计或结论正确性的问题(P1
   行为类)阻断合并;措辞、注释同步、文档一致性类**不阻断**,合并后追加修正。
2. **复核产出必须给二元判定**:每轮复核须明确写出「阻断 / 不阻断」;只列问题
   不给判定的复核不进入合并决策。
3. **已定稿的 harness**(`verify_formal_v3_5.sh` 与故障注入回归测试)非经功能性
   缺陷不再开新审查轮。
4. **与当前主线无关的发现**,一律写进披露登记(`docs/method/`)或 backlog issue
   批量处理,不触发新审查轮。
5. **审查轮数不是质量指标。** 主线(当前为文献检索任务 2)优先于一切打磨;
   复核发现的每个问题都"有道理"不构成阻塞主线的理由。
